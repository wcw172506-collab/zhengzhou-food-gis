# 郑州美食GIS系统 - 启动指南

## 系统概述

郑州美食GIS系统是一个基于Web的地理信息系统，用于展示和管理郑州市的餐饮数据。

### 技术栈
- **前端**: React + TypeScript + Ant Design + 百度地图
- **后端**: Node.js + Express + SQLite
- **数据库**: SQLite (better-sqlite3)

## 快速启动

### 方法一：使用启动脚本（推荐）

1. 双击运行 `启动系统.bat` 文件
2. 等待依赖安装完成（首次运行需要）
3. 浏览器自动打开 http://127.0.0.1:3000

### 方法二：手动启动

#### 启动后端服务器

```bash
cd backend
npm install  # 首次运行需要安装依赖
node server.js
```

后端将在 http://127.0.0.1:5000 运行

#### 启动前端服务器

打开新的终端窗口：

```bash
cd frontend
npm install  # 首次运行需要安装依赖
npm run dev
```

前端将在 http://127.0.0.1:3000 运行

## 配置百度地图密钥

### 必须配置！

系统使用百度地图显示餐厅点位，需要配置API密钥。

### 获取百度地图密钥

1. 访问百度地图开放平台：https://lbsyun.baidu.com/
2. 注册并登录账号
3. 进入"控制台" → "应用管理" → "我的应用"
4. 点击"创建应用"
5. 填写应用信息：
   - **应用名称**: 郑州美食GIS系统
   - **应用类型**: 浏览器端
   - **Referer白名单**: `*` （允许所有域名访问）
6. 提交后复制获取的 **AK（密钥）**

### 配置密钥

编辑文件：`frontend\src\pages\MapView.tsx`

找到第62行：
```typescript
script.src = 'https://api.map.baidu.com/api?v=3.0&ak=您的百度地图密钥'
```

将 `您的百度地图密钥` 替换为您实际获取的AK。

示例：
```typescript
script.src = 'https://api.map.baidu.com/api?v=3.0&ak=abcdefghijklmnopqrstuvwxyz123456'
```

### 保存并刷新

修改后保存文件，刷新浏览器页面即可看到地图。

## 系统功能

### 1. 地图浏览
- 显示郑州市地图
- 显示所有餐厅点位（红色标记）
- 点击标记查看餐厅详细信息
- 支持缩放、拖拽等地图操作

### 2. 餐厅搜索
- 关键词搜索
- 按区域筛选
- 按类型筛选
- 分页显示结果

### 3. 数据统计
- 餐厅总数统计
- 区域分布图表
- 类型分布图表
- 评分分布图表
- 价格分布图表

### 4. 用户管理（管理员）
- 查看用户列表
- 添加新用户
- 编辑用户信息
- 删除用户
- 修改用户角色

## 默认账号

系统初始化时会创建一个管理员账号：

- **用户名**: admin
- **密码**: admin123
- **角色**: 管理员

⚠️ **重要提示**: 首次登录后请立即修改默认密码！

## 端口说明

- **前端端口**: 3000
- **后端端口**: 5000

如果端口被占用，请修改以下文件：

### 修改前端端口
编辑 `frontend\vite.config.ts`:
```typescript
server: {
  port: 3000,  // 修改为其他端口
  // ...
}
```

### 修改后端端口
编辑 `backend\server.js`:
```javascript
const PORT = process.env.PORT || 5000;  // 修改为其他端口
```

## 常见问题

### 1. 地图不显示

**原因**: 未配置百度地图密钥

**解决**: 按照上述"配置百度地图密钥"章节获取并配置密钥

### 2. 端口被占用

**错误信息**: `Error: listen EADDRINUSE: address already in use`

**解决**:
- Windows: 查找并关闭占用端口的进程
  ```bash
  netstat -ano | findstr :3000
  taskkill /F /PID <进程ID>
  ```
- 或修改配置文件使用其他端口

### 3. 依赖安装失败

**原因**: 网络问题或npm源问题

**解决**:
```bash
# 切换到官方npm源
npm config set registry https://registry.npmjs.org/

# 或使用淘宝镜像
npm config set registry https://registry.npmmirror.com
```

### 4. 前端编译错误

**解决**:
```bash
cd frontend
rm -rf node_modules
rm package-lock.json
npm install
npm run dev
```

### 5. 数据库文件不存在

**原因**: 未导入数据

**解决**:
```bash
cd backend
node scripts/importData.js
```

## 数据导入

如果数据库为空，需要导入CSV数据：

```bash
cd backend
node scripts/importData.js
```

数据文件位于 `数据` 目录下，包含12个CSV文件，共19750条餐厅记录。

## API接口文档

### 认证接口

- `POST /api/auth/login` - 用户登录
- `POST /api/auth/register` - 用户注册

### 餐厅接口

- `GET /api/restaurants` - 获取餐厅列表（支持分页和筛选）
- `GET /api/restaurants/filters` - 获取筛选选项

### 统计接口

- `GET /api/statistics` - 获取统计数据

### 用户管理接口

- `GET /api/users` - 获取用户列表
- `POST /api/users` - 创建用户
- `PUT /api/users/:id` - 更新用户
- `DELETE /api/users/:id` - 删除用户

## 开发模式

### 前端开发

```bash
cd frontend
npm run dev     # 启动开发服务器
npm run build   # 构建生产版本
npm run preview # 预览生产构建
```

### 后端开发

```bash
cd backend
node server.js  # 启动服务器
```

## 生产部署

### 前端部署

```bash
cd frontend
npm run build
# 将 dist 目录部署到Web服务器
```

### 后端部署

```bash
cd backend
# 使用 PM2 或其他进程管理器
pm2 start server.js
```

## 系统要求

- Node.js: 16.x 或更高版本
- npm: 7.x 或更高版本
- 浏览器: Chrome、Firefox、Edge、Safari（现代浏览器）
- 网络: 需要访问百度地图API

## 技术支持

如有问题，请检查：
1. 控制台错误信息（F12打开开发者工具）
2. 后端服务器日志
3. 前端开发服务器日志

## 更新日志

### v1.0.0 (2026-01-17)
- 初始版本发布
- 实现地图浏览功能
- 实现餐厅搜索功能
- 实现数据统计功能
- 实现用户管理功能
- 集成百度地图
- 导入19750条餐厅数据

## 许可证

本项目仅供学习和研究使用。